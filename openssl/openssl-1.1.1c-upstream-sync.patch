diff -up crypto/rand/rand_lib.c.sync crypto/rand/rand_lib.c
--- crypto/rand/rand_lib.c.sync	2019-05-29 17:20:17.175099183 +0200
+++ crypto/rand/rand_lib.c	2019-05-30 11:51:20.784850208 +0200
@@ -232,8 +232,9 @@ size_t rand_drbg_get_nonce(RAND_DRBG *dr
     struct {
         void * instance;
         int count;
-    } data = { NULL, 0 };
+    } data;

+    memset(&data, 0, sizeof(data));
     pool = rand_pool_new(0, min_len, max_len);
     if (pool == NULL)
         return 0;